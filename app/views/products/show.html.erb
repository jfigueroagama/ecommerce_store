<h1 class="jumbotron"><%= "#{ @product.name.capitalize }" %><h1>
<div class="container">
  <div class="card">
    <div class="card-body">
      <div>
        <%= "#{@product.description}" %>
      </div>
      <div><small> SGD </small><%= "#{@product.price}" %></div>
    </div>
    <div class="card-footer">
      <%= link_to 'Back to Product List', products_path %>
    </div>
  </div>
</div>
